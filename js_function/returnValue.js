function tenTimes(number) {
    const result = number * 10;
    return result;
}

function cutHalf(num) {
    const half = num / 2;
    return half;
}

const output = tenTimes(5);
console.log(output);

const halfOutput = cutHalf(10);
console.log(halfOutput);
